<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobdeve.s16.desembrana.annapatricia.PressSOS">

    <uses-permission android:name="android.permission.SEND_SMS"></uses-permission>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="PressSOS"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".SetUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InfoActivity" />
        <activity android:name=".Info02Activity" />
        <activity android:name=".Info03Activity" />
        <activity android:name=".EditNameActivity" />
        <activity android:name=".EditNumActivity" />
        <activity android:name=".EditSOSActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AddEmergencyContactActivity" />
        <activity android:name=".EditEmergencyContactActivity" />
        <activity android:name=".LocationActivity" />
        <activity android:name=".EnterPinActivity" />
    </application>

</manifest>